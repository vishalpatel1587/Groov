import { RouteLabel, RoutPath } from '../../types/routes';

export const HeaderLinks = {
    [RouteLabel.CompanyRituals]: { name: RouteLabel, route: RoutPath.CompanyRituals },
    [RouteLabel.TeamRituals]: { name: RouteLabel, route: RoutPath.TeamRituals }
}